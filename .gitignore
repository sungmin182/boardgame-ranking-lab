# 원본 스냅샷/응답 캐시. 재실행하면 다시 만들어지므로 커밋하지 않는다.
cache/

# 중간 산출물 (games.json 만 커밋하면 사이트가 돈다)
data/ranks.json

node_modules/
_site/
.wrangler/
.DS_Store

# 내보낸 기록 백업 파일 (개인 자료 — 저장소에 올리지 않는다)
보드게임-내기록-*.json
